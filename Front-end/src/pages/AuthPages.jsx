import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import client from "../api/client";
import { useAuth } from "../context/AuthContext";
import { ErrorMessage } from "../components/Feedback";
export function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [error, setError] = useState(""); const submit = async (e) => { e.preventDefault(); setError(""); try { const user = await login(Object.fromEntries(new FormData(e.target))); navigate(user.role === "admin" ? "/admin" : "/"); } catch (err) { setError(err.response?.data?.message || "Unable to sign in"); } }; return <AuthCard title="Welcome back"><form onSubmit={submit}><input name="email" type="email" placeholder="Email address" required /><input name="password" type="password" placeholder="Password" required /><ErrorMessage error={error}/><button>Sign in</button></form><p>New here? <Link to="/register">Create an account</Link></p></AuthCard>; }
export function Register() { const navigate = useNavigate(); const [error, setError] = useState(""); const submit = async (e) => { e.preventDefault(); setError(""); try { await client.post("/auth/register", Object.fromEntries(new FormData(e.target))); navigate("/login"); } catch (err) { setError(err.response?.data?.message || "Unable to register"); } }; return <AuthCard title="Create your account"><form onSubmit={submit}><input name="name" placeholder="Full name" required /><input name="email" type="email" placeholder="Email address" required /><input name="password" type="password" minLength="8" placeholder="Password (8+ characters)" required /><ErrorMessage error={error}/><button>Create account</button></form><p>Already registered? <Link to="/login">Sign in</Link></p></AuthCard>; }
function AuthCard({ title, children }) { return <div className="auth-card"><p className="eyebrow">EVENTHUB</p><h1>{title}</h1>{children}</div>; }
