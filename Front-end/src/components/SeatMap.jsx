export default function SeatMap({ seats, selected = [], onToggle, readonly = false }) { return <div className="seat-map"><div className="screen">STAGE / SCREEN</div><div className="seat-grid">{seats.map((seat) => <button key={seat.id} disabled={readonly || seat.status === "booked"} onClick={() => onToggle?.(seat.id)} className={`seat ${seat.status} ${selected.includes(seat.id) ? "selected" : ""}`}>{seat.id}</button>)}</div><div className="legend"><span><i className="available" />Available</span><span><i className="selected" />Selected</span><span><i className="booked" />Booked</span></div></div>; }
